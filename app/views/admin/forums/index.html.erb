
<% @forums.each do |forum| %>
<h2><%= link_to(forum.title , admin_forum_posts_path(forum)) %></h2>
	<p><%= forum.context %> | <%= link_to("編輯" , edit_admin_forum_path(forum)) %> | <%= link_to("刪除" , [:admin , forum] , :confirm => "確定刪除？" ,  :method => :delete ) %></p>
<% end %>

<p><%= link_to("開辦新區" , new_admin_forum_path ) %></p>
