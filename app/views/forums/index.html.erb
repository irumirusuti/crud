<h1>Forum#index</h1>
<% @forums.each do |forum| %>
<h2><%= link_to (forum.title , forum_posts_path(forum)) %></h2>
	<p><%= forum.context %> | <%= link_to ("編輯" , edit_forum_path(forum)) %> | <%= link_to ("刪除" , forum , :confirm => "確定刪除？" ,  :method => :delete ) %></p>
<% end %>

<p><%= link_to ("開辦新區" , new_forum_path ) %></p>
